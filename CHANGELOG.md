# 更新日志

所有重要的项目变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.20] - 2026-09-02

### 新增
- Workflow 步骤支持启用/禁用和拖拽排序；禁用步骤会以弱化样式明确标识，拖拽时显示插入位置，原有上下移动操作继续保留。
- Workflow 请求日志详情新增全局变量设置结果，展示变量名、来源、取值以及 `set`、`failed`、`skipped` 状态和汇总数量。

### 变更
- HTTP Request 的 Code 页签改为展示解析环境变量、全局变量和内置变量后的 cURL，并让页面预览与 Copy 使用同一份命令；URL、查询参数、Header、鉴权、请求体、表单字段和文件路径均保持一致。
- Workflow 运行中的请求使用独立的 `running` 状态与高亮样式，日志详情标题在滚动时保持可见。

### 修复
- 修复 cURL 预览仅显示 `{{变量}}` 原始占位符、与实际复制和发送内容不一致的问题；无可用数据时仍保留原始占位符。
- 修复时间、随机数、GUID 等动态内置变量在 cURL 展示和复制之间可能变化的问题；同一次预览使用稳定快照。
- 修复查看或复制 cURL 时可能创建或推进 `$sequence` 的问题；预览现在只在内存中模拟序列步进，真实序列仅在请求执行时消费。
- 修复 Workflow 请求测试失败、变量取值无法解析或全局变量写入能力不可用时，日志未明确记录变量跳过或设置失败原因的问题。
